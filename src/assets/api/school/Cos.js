let url = '/proxy'

/* 设备管理模块所有接口 */

let Cos = {
	'region': url + '/api/region', // 获取所有区域
	'merchant': url + '/api/merchant', // 商户
	'pre': url + '/api/pre', // 前置
	'consumer': url + '/api/consumer', // 设备信息
	
}

export default Cos
